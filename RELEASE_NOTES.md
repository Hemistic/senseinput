# SenseVoice 语音输入 v0.1.1

这是当前 Windows x64 安装版，重点修复了浮窗生命周期、光标注入稳定性和发布流程问题。

## 主要更新

- **浮窗不再占用任务栏**：实时热键浮窗使用 Windows tool-window 样式，不再产生额外的任务栏按钮；系统托盘图标和设置入口保持不变。预览模式仍保留普通顶层窗口，方便截图和布局检查。
- **失焦注入不再导致闪退**：注入前校验根窗口、进程 ID 和 GUI 线程 ID，发现窗口被销毁、重建、跨进程复用或已经变成当前进程时立即停止写入。
- **UI Automation 防护**：对文本范围、选区、文档范围、Value/Text Pattern 以及更新后的光标对象增加空对象和失败结果检查，目标应用在切换焦点时只会安全回退到剪贴板。
- **TSF 降级处理**：TSF 会话初始化失败时继续尝试原生编辑框和 UI Automation，不再因为 COM、内存或文本服务提供程序异常退出主程序。
- **线程输入状态自动恢复**：使用 RAII 管理 `AttachThreadInput`，无论注入成功、失败还是异常，都保证解除线程附加，避免后续键盘焦点异常。
- **延迟注入会话隔离**：延迟 160 ms 的注入回调绑定录音会话编号，旧录音结束后的回调不会覆盖新会话或在程序关闭后访问已释放 UI。
- **连续注入回归覆盖**：增加连续两次注入、进程标识不匹配、目标窗口销毁后的安全失败测试。
- **标准安装向导**：发布包使用 Inno Setup 标准安装/卸载向导，支持当前用户安装、开始菜单快捷方式、可选开机启动和原生卸载程序。

## 验证结果

- Windows Release 构建通过：`sensevoice-ui`、`sensevoice-ui-legacy`、`sensevoice-stream`。
- CTest 全部通过：5/5。
- 浮窗几何回归通过：文本增长时窗口、文本气泡和底部胶囊中心保持稳定。
- 失效目标注入回归通过：窗口销毁后返回失败，不崩溃、不误写入其他窗口。

## 发布资产

- `SenseVoice-0.1.1-Setup.exe`：Windows x64 标准安装向导。

本版本默认使用 SenseVoiceSmall Q8 GGUF 和 FSMN-VAD，模型与推理均在本机执行。重新分发模型、运行时和第三方组件时，请遵守各上游项目及模型的许可条款。

# SenseVoice 语音输入 v0.1.0

首个公开 Windows 版本。

## 包含内容

- SenseVoiceSmall Q8 GGUF 离线识别
- FSMN-VAD 实时分句和可调阈值
- Qt 6 浮窗，显示实时文字、响度和 VAD 状态
- Windows TSF/UI Automation 光标注入
- 热词增强、口水词清理、标点和重复句整理
- 单句长度与工作集内存保护
- 录音期间暂时静音默认播放设备，结束后恢复
- 用户级安装程序和免安装便携版 ZIP

## 发布资产

- `SenseVoice-0.1.0-Setup.exe`：用户级安装包，默认注册当前用户开机启动
- `SenseVoice-0.1.0-windows-x64.zip`：免安装便携版

## 注意事项

- 当前 Release 仅支持 Windows x64。
- 默认使用 SenseVoiceSmall Q8，模型和运行时均在本机执行。
- 重新分发模型、运行时和第三方组件时，请遵守各上游项目及模型的许可条款。
